<tr>
  <th></th>
  <th title="Average number of points">Average Points</th>
  <th title="Total entries">Total Entries</th>
  <th title="Percent that have a value of 2">Percent Perfect</th>
</tr>
<% students.each do |username, student| %>
<tr>
  <th><%= link_to student[:user].username, group_member_path(@group, student[:user]) %></th>
  <%= render partial: "memberships/data_row", locals: {collection: student[:memberships].collect{|m| m.send(datum)}.flatten } %>
</tr>
<% end %>
